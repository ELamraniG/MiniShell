next
// syntax error, redirection after pipe is not syntax error
// handle ton of pipes
//add the default vars into env when env -i


//at the end add wall extra and remove sanitizer
// make < 4 do like WORD in exectree

problem with exit
problem with cd

wildcards for files
change back fork to return when failure